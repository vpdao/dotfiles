- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.shell: 
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc
    ~/.arm:
    ~/.zsh:
    ~/.bash:
    ~/.tmux.conf: tmux.conf
- shell:
  - [git submodule update --init --recursive, Installing submodules]
