# Use colors in coreutils utilities output
alias ls='ls --color=auto'
alias grep='grep --color'

# ls aliases
alias ll='ls -lah'
alias la='ls -A'
alias l='ls'

# Aliases to protect overwritting
alias cp='cp -i'
alias mv='mv -i'

# git related aliases
alias gs='git status'
alias gc='git commit'
alias gp='git push'
alias gb='git branch'
alias gbr='git branch -r'
alias gd='git diff'
alias ga='git add'
alias gr='git rebase'

# fd alias
alias fd='fdfind'
